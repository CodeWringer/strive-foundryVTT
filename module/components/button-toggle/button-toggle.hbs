{{!-- Required context:
viewModel: {ButtonToggleViewModel}
cssClass: {String | undefined}
--}}
<button 
  class="custom-system-button component-button-toggle{{#if viewModel.value}} active{{/if}} {{cssClass}}" 
  id="{{viewModel.id}}" 
>
  <span class="component-button-toggle-knob{{#if viewModel.value}} active{{/if}}"></span>
</button>