{{!-- Required context:
viewModel: {InputImageViewModel}
cssClass: {String | undefined}
--}}
<img 
  class="custom-system-edit component-input-image {{cssClass}}" 
  id="{{viewModel.id}}" 
  src="{{viewModel.value}}" 
  title="{{viewModel.title}}" 
  height="{{viewModel.width}}" 
  width="{{viewModel.height}}" 
  />