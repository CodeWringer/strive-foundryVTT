{{!-- Required context:
viewModel: InputTextFieldViewModel
cssClass: String | undefined
--}}
<div id="{{viewModel.id}}" class="strive textfield flex flex-middle strive-regular-font{{#if cssClass}} {{cssClass}}{{/if}}">
  {{!-- Edit mode --}}
  <input 
    class="edit-mode{{#if viewModel.isEditable}}{{else}} hidden{{/if}}"
    type="text"
    name="{{viewModel.id}}"
    placeholder="{{viewModel.placeholder}}"
    value="{{viewModel.value}}"
  />
  {{!-- Read mode --}}
  <div 
    class="read-mode{{#if viewModel.isEditable}} hidden{{/if}}" 
    name="{{viewModel.id}}"
  >{{viewModel.value}}</div>
</div>