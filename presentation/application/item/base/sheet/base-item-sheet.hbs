{{!-- context:
viewModel: ViewModel
--}}
<section id="{{viewModel.id}}" class="{{viewModel.cssClass}}">
  {{#with viewModel.contentTemplate as | template |}}
  {{> (template) viewModel=../viewModel }}
  {{/with}}
</section>