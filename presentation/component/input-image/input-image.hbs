{{!-- Required context:
viewModel: {InputImageViewModel}
cssClass: {String | undefined}
--}}
<img 
  class="custom-system-edit{{#if viewModel.isEditable}}{{else}} disabled{{/if}}{{#if cssClass}} {{cssClass}}{{/if}}" 
  id="{{viewModel.id}}" 
  src="{{viewModel.value}}" 
  title="{{viewModel.localizedTitle}}"
  />