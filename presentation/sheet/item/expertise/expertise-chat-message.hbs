{{!-- Parameters:
viewModel: {ExpertiseChatMessageViewModel}
cssClass: {undefined | String}
--}}
<section class="custom-system-chat-message{{#if cssClass}} {{cssClass}}{{/if}}" 
  id="{{viewModel.id}}"
  data-view-model-id="{{viewModel.id}}"
  data-document-id="{{viewModel.owningDocumentId}}"
  data-expertise-id="{{viewModel.expertise.id}}"
>
  {{!-- Header --}}
  {{#if viewModel.showParentSkill}}
    <div class="flex flex-row">
      {{#if viewModel.showParentSkillImage}}
      <div class="flex flex-center">
        <img class="inline-img header-image margin-r-sm" name="img" src="{{viewModel.parentSkillImage}}" height="20" width="20" />
      </div>
      {{/if}}
      {{#if viewModel.showParentSkillName}}
        {{#> header3 showFancyFont=viewModel.showFancyFont}}{{viewModel.parentSkillName}}{{/header3}}
      {{/if}}
    </div>
  {{/if}}
  <div class="flex flex-row">
    <div class="flex flex-center">
      <img class="inline-img header-image margin-r-sm" name="img" src="{{viewModel.expertise.img}}" title="{{viewModel.expertise.name}}" height="26" width="26" />
    </div>
    {{#> header2 showFancyFont=viewModel.showFancyFont cssClass="flex-grow"}}{{viewModel.expertise.name}}{{/header2}}
  </div>
  {{!-- Required level --}}
  <div class="flex flex-row sheet-block margin-b-md">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.character.skill.expertise.requirement"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.expertise.requiredLevel}}
    </div>
  </div>
  {{!-- apCost --}}
  <div class="flex flex-row sheet-block margin-b-md">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.character.skill.expertise.apCost"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.expertise.apCost}}
    </div>
  </div>
  {{!-- Obstacle --}}
  <div class="flex flex-row sheet-block margin-b-md{{#if viewModel.hideObstacle}} hidden{{/if}}">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.roll.obstacle.abbreviation"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.expertise.obstacle}}
    </div>
  </div>
  {{!-- Opposed By --}}
  <div class="flex flex-row sheet-block margin-b-md{{#if viewModel.hideOpposedBy}} hidden{{/if}}">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.roll.obstacle.opposedBy.label"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.expertise.opposedBy}}
    </div>
  </div>
  {{!-- Condition --}}
  <div class="flex flex-row sheet-block margin-b-md{{#if viewModel.hideCondition}} hidden{{/if}}">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.character.skill.expertise.condition.label"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{localize viewModel.expertise.condition}}
    </div>
  </div>
  {{!-- attackType --}}
  <div class="flex flex-row sheet-block margin-b-md{{#if viewModel.hideAttackType}} hidden{{/if}}">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.attackType.label"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.localizedAttackType}}
    </div>
  </div>
  {{!-- distance --}}
  <div class="flex flex-row sheet-block margin-b-md{{#if viewModel.hideDistance}} hidden{{/if}}">
    <div class="width-min-xxl">
      {{#> label showFancyFont=viewModel.showFancyFont}}{{localize "system.character.skill.expertise.distance.label"}}{{/label}}
    </div>
    <div class="flex-grow">
      {{viewModel.expertise.distance}}
    </div>
  </div>
  {{!-- Damage --}}
  <ol class="unstyled-list{{#if viewModel.hideDamage}} hidden{{/if}}">
    {{#each viewModel.expertise.damage as |entry index|}}
      <li class="sheet-block flex flex-column margin-b-md">
        {{!-- Damage formula --}}
        <div class="flex flex-row">
          <div class="width-min-xxl{{#if ../viewModel.showFancyFont}} fancy-font{{/if}}">
            {{localize "system.damageDefinition.formula"}}
          </div>
          <span class="width-min-xxl">
            {{entry.damage}}
          </span>
        </div>
        {{!-- Damage type --}}
        <div class="flex flex-row">
          <div class="width-min-xxl{{#if ../viewModel.showFancyFont}} fancy-font{{/if}}">
            {{localize "system.damageType.label"}}
          </div>
          <span class="width-xxl">
            {{localize entry.damageType.localizableName}}
          </span>
        </div>
      </li>
    {{/each}}
  </ol>
  {{!-- Description --}}
  <div class="sheet-block">
    {{> lazyRichText viewModel=viewModel.vmLazyDescription}}
  </div>
</section>