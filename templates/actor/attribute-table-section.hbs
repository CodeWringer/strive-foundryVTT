<section>
    <table class="attribute-table">
        <tbody>
            <tr class="fancy-font">
                <th class="pad-sm">
                    {{localize attGroup.name}}
                </th>
                <th class="pad-sm">
                    {{localize "ambersteel.labels.level"}}
                </th>
                <th class="pad-sm">
                    {{localize "ambersteel.labels.requiredSuccessFailureAbbreviation"}}
                </th>
                <th class="pad-sm">
                    {{localize "ambersteel.labels.successAbbreviation"}}
                </th>
                <th class="pad-sm">
                    {{localize "ambersteel.labels.failureAbbreviation"}}
                </th>
            </tr>
            {{#each atts as |att|}}
            <tr class="item" data-att-group-name="{{attGroup.name}}">
                <td class="pad-sm fancy-font flex">
                    <span class="flex-grow">{{localize att.name}}</span>
                    <span>[{{localize att.abbreviation}}]</span>
                </td>
                <td class="pad-sm input-slim">
                    <input class="attribute-value inline-edit-attribute" name="att.data.value" type="number" value="{{att.data.value}}" />
                </td>
                <td class="pad-sm">
                    {{att.requiredSuccessses}} / {{att.requiredFailures}}
                </td>
                <td class="pad-sm input-slim">
                    <input name="att.data.successes" type="number" value="{{att.data.successes}}" />
                </td>
                <td class="pad-sm input-slim">
                    <input name="att.data.failures" type="number" value="{{att.data.failures}}" />
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</section>