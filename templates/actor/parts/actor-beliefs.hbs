{{!-- Parameters:
viewModel: {ActorBeliefsViewModel}
cssClass: {undefined | String}
--}}
<section class="{{cssClass}}">
  <div class="sheet-block margin-b-md">
    <h3>
      <label class="{{#if viewModel.showFancyFont}}fancy-font{{/if}}">{{localize "ambersteel.character.beliefSystem.ambition"}}</label>
    </h3>
    <div class="margin-b-md">
      {{> inputTextField viewModel=viewModel.vmTfAmbition }}
    </div>
  </div>
  <div class="sheet-block margin-b-md">
    <h3>
      <label class="{{#if viewModel.showFancyFont}}fancy-font{{/if}}">{{localize "ambersteel.character.beliefSystem.belief.plural"}}</label>
    </h3>
    {{#each viewModel.beliefViewModels as |vm index|}}
      <div class="margin-b-md">
        {{> inputTextField viewModel=vm}}
      </div>
    {{/each}}
  </div>
  <div class="sheet-block margin-b-md">
    <h3>
      <label class="{{#if viewModel.showFancyFont}}fancy-font{{/if}}">{{localize "ambersteel.character.beliefSystem.instinct.plural"}}</label>
    </h3>
    {{#each viewModel.instinctViewModels as |vm index|}}
      <div class="margin-b-md">
        {{> inputTextField viewModel=vm}}
      </div>
    {{/each}}
  </div>
</section>