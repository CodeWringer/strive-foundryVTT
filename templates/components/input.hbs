{{!-- Required context:
isEditable: {Boolean}
itemId: {String|undefined}
propertyPath: {String}
type: {String} = "single-line|multi-line|drop-down|number"
cssClass: {String|undefined}
readOnlyCssClass: {String|undefined}
name: {String|undefined}
contextOverride: {Any}
-- Drop-down only
options: {Array<Object>|undefined}
optionValueProperty: {String|undefined}
optionDisplayProperty: {String|undefined}
--}}
{{#if isEditable}}
  {{!-- Single-line input --}}
  {{#if (eq type 'single-line')}}
    <input class="ambersteel-edit {{cssClass}}"
      data-item-id="{{itemId}}"
      data-property-path="{{propertyPath}}"
      type="text"
      name="{{name}}"
      value="{{lookupValue (isDefined contextOverride this) propertyPath itemId}}"
    />
  {{/if}}
  {{!-- Drop-down --}}
  {{#if (eq type 'drop-down')}}
    <select class="ambersteel-edit drop-down {{cssClass}}"
      data-item-id="{{itemId}}"
      data-property-path="{{propertyPath}}"
      name="{{name}}"
    >
      {{#select propertyPath}}
      {{#each options as |option index|}}
      <option value="{{lookupValue option ../optionValueProperty}}">{{localize (lookupValue option ../optionDisplayProperty)}}</option>
      {{/each}}
      {{/select}}
    </select>
  {{/if}}
  {{!-- Number-spinner --}}
  {{#if (eq type 'number')}}
    {{#> numberSpinner}}
    <input class="ambersteel-edit spinner {{cssClass}}"
      type="number"
      data-dtype="Number"
      min="0"
      step="1"
      data-item-id="{{itemId}}"
      data-property-path="{{propertyPath}}"
      value="{{lookupValue (isDefined contextOverride this) propertyPath itemId}}"
      name="{{name}}"
      />
    {{/numberSpinner}}
  {{/if}}
  {{!-- Textarea --}}
  {{#if (eq type 'multi-line')}}
      <textarea class="ambersteel-edit {{cssClass}}"
        spellcheck="false"
        data-item-id="{{itemId}}"
        data-property-path="{{propertyPath}}"
        name="{{name}}"
      >{{lookupValue (isDefined contextOverride this) propertyPath itemId}}</textarea>
  {{/if}}
{{else}}
  {{!-- Display read-only value --}}
  <span class="ambersteel-read-only {{readOnlyCssClass}}" name="{{name}}">
    {{lookupValue (isDefined contextOverride this) propertyPath itemId}}
  </span>
{{/if}}