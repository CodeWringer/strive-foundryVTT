{{!-- Requires context:
isEditable: {Boolean}
isSendable: {Boolean}
itemId: {String|undefined}
item: {Item} = {
  id: {String}
  name: {String}
  img: {String}
  data: {ItemData} = {
    data: {Object} = {
      description: {String}
      gmNotes: {String}
      isCustom: {Boolean}
      quantity: {Number}
      maxQuantity: {Number}
      bulk: {Number}
      shape: {String} = "straight"|"square"
      isOnPerson: {Boolean}
    }
  }
}
--}}
{{!-- Row 1 --}}
<div class="flex flex-row">
  <div class="flex flex-grow">
    {{!-- Send to chat --}}
    {{> buttonSendToChat itemId=itemId }}
    {{!-- Name --}}
    <span class="width-min-xl">
      {{> inputLabel text="ambersteel.labels.item"}}
    </span>
    <span class="flex-grow margin-r-md">
      {{> inputComponent type="single-line" propertyPath="name" itemId=itemId isEditable=isEditable }}
    </span>
  </div>
  {{!-- Delete --}}
  {{> buttonDelete itemId=itemId }}
</div>
{{!-- Row 2 --}}
<div class="flex flex-row">
  {{!-- Bulk --}}
  <div class="flex flex-row">
    <span class="width-min-xl">
      {{> inputLabel text="ambersteel.labels.bulk" }}
    </span>
    <span class="width-md">
      {{> inputComponent type="number" propertyPath="data.data.bulk" min="1" itemId=itemId isEditable=isEditable }}
    </span>
  </div>
  {{!-- Spacer --}}
  <div class="width-xs"></div>
  {{!-- Quantity --}}
  <div class="flex flex-row flex-grow">
    <span class="width-min-xl">
      {{> inputLabel text="ambersteel.labels.quantity" }}
    </span>
    <span class="width-min-xl flex-grow">
      {{> inputComponent type="number" propertyPath="data.data.quantity" min="1" max=(lookupValue this "data.data.maxQuantity" itemId) itemId=itemId isEditable=isEditable }}
    </span>
  </div>
  {{!-- Spacer --}}
  <div class="width-xs"></div>
  {{!-- Max quantity --}}
  <div class="flex flex-row flex-grow">
    <span class="width-min-xl">
      {{> inputLabel text="ambersteel.labels.maxQuantity" }}
    </span>
    <span class="width-min-xl flex-grow">
      {{> inputComponent type="number" propertyPath="data.data.maxQuantity" min="1" itemId=itemId isEditable=isEditable }}
    </span>
  </div>
</div>
{{!-- Row 3 --}}
{{!-- Description --}}
<div class="flex flex-row">
  {{> inputComponent type="multi-line" propertyPath="data.data.description" itemId=itemId isEditable=isEditable }}
</div>